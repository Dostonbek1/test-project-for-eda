---
- name: Run a webhook listener service
  hosts: localhost
  sources:
    - name: webhook_listener
      ansible.eda.webhook:
        host: 0.0.0.0
        port: 5000
  rules:
    - name: Webhook event
      condition: event is not None
      action: none